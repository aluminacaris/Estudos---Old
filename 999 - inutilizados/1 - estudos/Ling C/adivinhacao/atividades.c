#include <stdio.h>

int main() {
    float banana = 1.5;
    int coca = 3;
    float total = banana * coca;
    printf("total é %.2f\n", total);


    // int soma = 0; // Inicializa a variável soma

    // // Programa que calcula a soma de todos os números de 1 a 100
    // for (int i = 1; i <= 100; i++) {
    //     soma += i; // Adiciona o valor de i à soma
    // }

    // // Imprime o resultado
    // printf("A soma de todos os números de 1 a 100 eh: %d\n", soma);

    // return 0;
}
